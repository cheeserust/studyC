#include <stdio.h>

typedef struct {
    double left;
    double right;
}WheelSpeed;

typedef struct{
    double linear;
    double angular;
}RobotSpeed;

RobotSpeed computeSpeed(WheelSpeed w, double wheel_radius, double wheel_distance) {}



int main()
{

    return 0;
}